Publishme.cc
============

run in dev:

*pyton3 required*

`$ make run-dev`

* if you have python2 as default, edit Makefile to use your python3



run in docker:

*docker-compose required*

`$ make run-prod`
