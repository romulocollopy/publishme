version: '3.3'
services:
  app:
    build: .
    ports:
    - "5000:5000"
    volumes:
    - .:/opt/app
    # links:
    # - db
    # - webserver
  # db:
    # image: mongo:3.2.16
  # webserver:
    # image: nginx:latest
